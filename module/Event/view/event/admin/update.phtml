<?php
/* @var $event Event\Entity\EventEntity */
$event = $this->event;

/* @var $eventForm Event\Form\EventForm */
$eventForm = $this->eventForm;
$eventForm->prepare();
$eventForm->setAttribute(
    'action', $this->url(
        'event-admin/action',
        array('action' => 'update', 'id' => $event->getId())
    )
);
foreach ($eventForm->getElements() as $element) {
    $element->setLabelAttributes(
        array('style' => 'display: block; width: 200px;')
    );
    $element->setAttributes(array('style' => 'display: block; width: 400px;'));
}
?>
<h1>Event bearbeiten</h1>
<?php if ($this->message): ?>
    <div class="alert alert-danger"><?php echo $this->message ?></div>
<?php endif; ?>
<?php echo $this->form($eventForm); ?>

<p>
    <a class="btn btn-default" href="<?php echo $this->url('event-admin'); ?>">
        zurück
    </a>
</p>
